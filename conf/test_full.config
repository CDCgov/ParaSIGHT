/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config file for running full-size tests
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Defines input files and everything required to run a full size pipeline test.

    Use as follows:
        nextflow run wdpb/parasight -profile test_full,<docker/singularity> --outdir <OUTDIR>

----------------------------------------------------------------------------------------
*/

params {
    config_profile_name        = 'Full test profile'
    config_profile_description = 'Full test dataset to check pipeline function'

    // Input data for full size test
    // TO-DO: update input filepaths to be abstracted and not hard-coded to CDC data
    input  = '/scicomp/groups-pure/OID/NCEZID/DFWED/WDPB/MEPI/ngr8/dev/parasight/assets/test_full.samplesheet.csv'
    reference = '/scicomp/groups-pure/OID/NCEZID/DFWED/WDPB/MEPI/ngr8/assets/reference_alleles.fasta'
    skip_eggnog = true
}
